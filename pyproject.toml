[project]
name = "seitenrechner"
version = "1.0.0"
description = "Seitenkomplementrechner für den Druck"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
build = [
    "pyinstaller>=6.0",
]

[tool.uv]
dev-dependencies = [
    "pyinstaller>=6.0",
]
